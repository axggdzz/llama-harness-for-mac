//! LlamaHarness macOS port entry point.
//!
//! The Windows C# implementation lives at the repository root.  All Rust/macOS
//! code is intentionally scoped to `for-mac/` so the two implementations remain
//! independently buildable during the migration.

fn main() {
    println!("llama-harness-mac: implementation pending; see docs/spec.md");
}
