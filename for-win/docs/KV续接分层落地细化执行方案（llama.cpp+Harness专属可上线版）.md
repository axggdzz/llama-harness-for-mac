# KV续接分层落地细化执行方案（llama\.cpp\+Harness专属可上线版）

## 一、方案总定位与架构分工

### 1\.1 核心定位

解决 Trae Work 超长输出截断续接痛点，摒弃原生全量重算的低效逻辑，适配 **llama\.cpp 内核特性限制**，采用「短期稳态兜底 \+ 长期独家真续接」双阶段落地路线，与现有 TokenGuard 输入超限防护、10G RAMDisk KV 持久化、双槽位亲和调度完全联动闭环。

整体实现：**输入不超限、输出截断无损续接、超长会话零崩溃、续接最小重算、稳态稳定性拉满**。

### 1\.2 分层架构分工（严格解耦）

- **llama\.cpp 底层**：负责推理执行、原生 Slot 缓存、KV 快照读写、输出截断判定（finish\_reason=length），不做任何定制魔改，保持上游兼容性

- **Harness 调度层（核心改造层）**：统一承接续接逻辑、分支路由、KV 快照管控、异常容错、Token 统计、会话状态锁定，所有续接能力内聚在调度层

- **前端 Trae Work**：无感适配，无需改造，沿用原生交互逻辑，自动享受续接性能收益与稳定性提升

### 1\.3 阶段落地总纲

1. **第一阶段（当前迭代）**：Harness 智能代理自动续接（稳态可用、零风险、全场景兼容）

2. **第二阶段（稠密优化收官后）**：自研 llama\.cpp 专属 KV 真续接（生态独家、近似零重算、极致性能）

## 二、第一阶段：Harness 智能代理自动续接细化方案（可直接开发上线）

### 2\.1 核心能力边界

规避原版开源代理方案的所有短板，适配 llama\.cpp 全业务场景，实现**可控、稳定、可观测、不错乱**的自动续接：

- 非流式、非工具调用场景：全自动续接，用户无感

- 流式输出、ToolCall 工具调用场景：自动透传，交由 Trae 原生处理，杜绝工具链断裂、流式分片错乱问题

- 联动 TokenGuard 输入裁剪，从源头避免超限崩溃，续接仅用于输出超长截断场景

### 2\.2 完整执行流程

#### 2\.2\.1 请求前置校验（入口拦截）

Harness ForwardAsync 路由后、转发模型前执行：

1. 识别请求类型：区分流式/非流式、是否携带 tools 参数

2. 合规场景判定：仅 **非流式 \+ 无工具调用** 进入自动续接分支

3. 超限防护：复用 TokenGuard 预算规则，拦截输入超限请求，避免无效推理与截断

#### 2\.2\.2 首轮推理执行

1. 透传用户原始请求参数，保留原始 max\_tokens、温度、采样参数不变

2. 接收模型返回结果，捕获 finish\_reason 字段

3. 分支判定：
        

    - finish\_reason \!= length：正常结束，直接返回结果，不触发续接

    - finish\_reason == length：输出截断，进入自动续接循环逻辑

#### 2\.2\.3 续接循环逻辑（核心）

1. 内容拼接：保存本轮已生成完整内容，避免重复丢失

2. 上下文回填：将本轮 assistant 输出追加至 messages 历史末尾

3. 续接指令注入：追加标准化兜底续接 Prompt `请继续输出，不要重复已有内容，延续上文逻辑完成剩余内容`

4. 循环重试：复用更新后的 messages 发起下一轮推理

5. 终止条件（满足任一即退出）：
        

    - 推理正常结束（finish\_reason=stop）

    - 达到最大续接迭代次数（默认10次，可配置）

    - 检测内容无增量更新（防死循环）

#### 2\.2\.4 结果归一化与字段修复

解决开源代理方案 usage 统计失真、返回体异常问题：

- 拼接多轮生成完整内容，统一覆盖最终返回 message\.content

- 累加每轮 completion\_tokens、prompt\_tokens，修正总 usage 数据，保证统计精准

- 强制统一 finish\_reason 为 stop，前端无感无报错

### 2\.3 核心风控与容错机制

- **死循环防护**：最大迭代次数可配置 \+ 内容哈希校验，连续两轮内容一致自动终止

- **超时防护**：单轮推理超时300s，全局续接流程超时自动退出，返回已拼接有效内容

- **异常降级**：模型报错、网络异常时，直接返回已生成内容，不阻断业务

- **工具场景隔离**：只要检测到 tools/tool\_calls 参数，直接透传，不进入续接逻辑，杜绝工具上下文断裂

- **流式场景隔离**：SSE 流式请求全程透传，规避分片错乱、首尾重复问题

### 2\.4 可观测日志规范

所有续接行为全程留痕，适配 M4 可观测体系：

- 触发日志：记录会话 ID、触发截断轮次、单轮生成 Token 数

- 迭代日志：记录续接迭代次数、每轮耗时、累计生成 Token 数

- 终止日志：记录终止原因（正常结束/达最大次数/无增量/超时）

## 三、第二阶段：llama\.cpp 专属自研 KV 真续接细化方案（远期终极形态）

针对 llama\.cpp 无原生增量前缀续接的内核短板，基于现有 **Slot 快照 \+ RAMDisk 持久化** 架构，自研生态独家真续接能力，抹平与 vLLM 的续接性能差距。

### 3\.1 核心原理

利用 llama\.cpp 官方 `/slots/0?action=save/restore` 接口，结合内存盘高速读写特性，实现**截断态 KV 快照留存 \+ 增量接续解码**，规避全量 Prefill 重算。

### 3\.2 完整执行流程

#### 3\.2\.1 截断瞬间自动快照留存

推理结束检测到 `finish_reason=length` 时，立即触发后置钩子：

1. 锁定当前会话绑定槽位，禁止抢占、驱逐、缓存冲刷

2. 调用 save 接口，将**截断断点完整 KV 状态**写入 RAMDisk 快照文件

3. 绑定会话 SessionID 与快照文件，建立专属映射关系

4. 标记槽位为「截断待续接状态」，维持上下文现场不销毁

#### 3\.2\.2 续接请求增量恢复

同一 SessionID 发起继续请求时，优先命中快照逻辑：

1. 校验会话存在有效截断快照，且槽位状态合法

2. 调用 restore 接口，高速加载截断点 KV 缓存（1\.4s 极速恢复）

3. 仅下发极简增量续接指令，无需携带完整历史消息

4. 模型基于原有 KV 状态直接增量 Decode，**完全跳过全量 Prefill**

#### 3\.2\.3 快照生命周期管理

- 续接成功、正常结束后，自动清理过期截断快照，释放内存盘空间

- 长期无续接请求，触发 LRU 淘汰机制，自动回收冷快照

- 主力业务快照永久保留，临时会话快照限时回收

### 3\.3 核心收益（对标开源天花板）

- 彻底解决 Trae 原生续接 **10s\+ 全量重算** 痛点，续接耗时压缩至 2s 以内

- 无人工续接 Prompt 污染，语义连续性完美，无内容重复、逻辑断层

- 内存盘高速读写，快照存取零磁盘 IO 抖动，稳态性能拉满

- llama\.cpp 生态独家能力，碾压所有通用开源部署方案

## 四、双方案联动兼容规则

1. 第一阶段代理续接、第二阶段 KV 真续接**双向兼容、可无缝切换**

2. 真续接上线后，自动优先启用快照增量续接，异常时自动降级为代理续接

3. 所有场景均与 TokenGuard 输入防护、显存稳态治理联动，形成闭环防护

## 五、上线验收量化指标

### 5\.1 第一阶段验收标准

- 非流式长文本截断场景 100% 自动续接成功，无内容丢失、无重复错乱

- 流式、工具调用场景零异常、零错乱、零崩溃

- usage Token 统计精准，无失真、无遗漏

- 无死循环、无超时阻塞、无服务中断

### 5\.2 第二阶段验收标准

- 超长输出续接**零全量 Prefill**，纯增量解码

- 续接速度较原生方案提升 5 倍以上

- 截断快照留存、恢复、清理生命周期闭环，无内存盘垃圾堆积

- 长会话稳态运行无崩溃、无显存溢出、无性能衰减

## 六、最终落地结论

本方案彻底规避原版 vLLM 专属 KV 续接的适配短板，贴合 llama\.cpp 内核特性与现有 Harness 架构，**短期稳保可用、长期极致碾压**。第一阶段快速解决业务痛点，第二阶段打造生态独家技术壁垒，完全匹配稠密模型极致优化的迭代路线，最终实现个人本地部署长会话续接的性能与稳定性天花板。

> （注：部分内容可能由 AI 生成）
